Use the `debugger` agent to diagnose and fix the issue described by the user (or the most recent error/traceback visible in context).

Provide the debugger agent with:
- The error message or unexpected behavior
- The file(s) most likely involved (if known)
- Any relevant context from this conversation

The debugger agent will reproduce, locate the root cause, fix it, and verify with `make test`.
